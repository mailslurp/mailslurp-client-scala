/**
 * MailSlurp API
 * MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
 *
 * The version of the OpenAPI document: 6.5.2
 * Contact: contact@mailslurp.dev
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package com.mailslurp.apis

import com.mailslurp.modelspackageVersion&#x3D;17.4.0.AttachmentMetaData
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.ByteArray
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.CanSendEmailResults
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.CheckEmailBodyFeatureSupportResults
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.CheckEmailBodyResults
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.CheckEmailClientSupportOptions
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.CheckEmailClientSupportResults
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.ContentMatchOptions
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.CountDto
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.DownloadAttachmentDto
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.Email
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailAuditAnalysisResult
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailAuditDto
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailContentMatchResult
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailContentPartResult
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailHtmlDto
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailLinksResult
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailPreview
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailPreviewUrls
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailScreenshotResult
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailSignatureParseResult
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailTextLinesResult
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailThreadDto
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.EmailThreadItemsDto
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.ExtractCodesOptions
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.ExtractCodesResult
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.ForwardEmailOptions
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.GetEmailScreenshotOptions
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.GravatarUrl
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.ImapFlagOperationOptions
import java.time.OffsetDateTime
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.PageEmailProjection
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.PageEmailThreadProjection
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.RawEmailJson
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.ReplyToEmailOptions
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.SearchEmailsOptions
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.SendEmailOptions
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.SentEmailDto
import java.util.UUID
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.UnreadCount
import com.mailslurp.modelspackageVersion&#x3D;17.4.0.ValidationDto
import com.mailslurp.client.core.JsonSupport._
import sttp.client._
import sttp.model.Method

object EmailControllerApi {

def apply(baseUrl: String = "https://api.mailslurp.com") = new EmailControllerApi(baseUrl)
}

class EmailControllerApi(baseUrl: String) {

  /**
   * Applies RFC3501 IMAP flag operations on a message. Current implementation supports read/unread semantics via the `\\\\Seen` flag only.
   * 
   * Expected answers:
   *   code 200 : EmailPreview (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   * @param imapFlagOperationOptions 
   */
  def applyImapFlagOperation(apiKey: String)(emailId: UUID, imapFlagOperationOptions: ImapFlagOperationOptions
): Request[Either[ResponseError[Exception], EmailPreview], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/${emailId}/imap-flag-operation")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .body(imapFlagOperationOptions)
      .response(asJson[EmailPreview])

  /**
   * Validates sender/inbox context and recipient eligibility before attempting a send. Useful for preflight checks in UI or test workflows.
   * 
   * Expected answers:
   *   code 200 : CanSendEmailResults (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param inboxId ID of the inbox you want to send the email from
   * @param sendEmailOptions 
   */
  def canSend(apiKey: String)(inboxId: UUID, sendEmailOptions: SendEmailOptions
): Request[Either[ResponseError[Exception], CanSendEmailResults], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/can-send?inboxId=${ inboxId }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .body(sendEmailOptions)
      .response(asJson[CanSendEmailResults])

  /**
   * Runs the same message-level audit bundle used by the email audit dashboard in one request. Combines content checks, HTML validation, compatibility analysis, and reputation verdict rollup when available.
   * 
   * Expected answers:
   *   code 200 : EmailAuditAnalysisResult (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   */
  def checkEmailAudit1(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], EmailAuditAnalysisResult], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/${emailId}/check-email-audit")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailAuditAnalysisResult])

  /**
   * Runs content quality checks against hydrated email body content. This endpoint performs outbound HTTP checks for linked resources, so avoid use with sensitive or stateful URLs.
   * 
   * Expected answers:
   *   code 200 : CheckEmailBodyResults (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   */
  def checkEmailBody(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], CheckEmailBodyResults], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/${emailId}/check-email-body")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[CheckEmailBodyResults])

  /**
   * Detects HTML/CSS features in the target email body and reports compatibility across major email clients and devices.
   * 
   * Expected answers:
   *   code 200 : CheckEmailBodyFeatureSupportResults (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   */
  def checkEmailBodyFeatureSupport(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], CheckEmailBodyFeatureSupportResults], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/${emailId}/check-email-body-feature-support")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[CheckEmailBodyFeatureSupportResults])

  /**
   * Evaluates HTML/CSS features in the supplied body and reports support coverage across major email clients and platforms.
   * 
   * Expected answers:
   *   code 200 : CheckEmailClientSupportResults (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param checkEmailClientSupportOptions 
   */
  def checkEmailClientSupport(apiKey: String)(checkEmailClientSupportOptions: CheckEmailClientSupportOptions
): Request[Either[ResponseError[Exception], CheckEmailClientSupportResults], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/check-email-client-support")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .body(checkEmailClientSupportOptions)
      .response(asJson[CheckEmailClientSupportResults])

  /**
   * Runs the aggregate audit bundle for the target email and stores the resulting audit record for later review and history tracking.
   * 
   * Expected answers:
   *   code 201 : EmailAuditDto (Created)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   */
  def createEmailAuditForEmail(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], EmailAuditDto], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/${emailId}/audit")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailAuditDto])

  /**
   * Deletes all emails for the authenticated account context. This operation is destructive and cannot be undone.
   * 
   * Expected answers:
   *   code 204 :  (No Content)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   */
  def deleteAllEmails(apiKey: String)(
): Request[Either[ResponseError[Exception], Unit], Nothing] =
    basicRequest
      .method(Method.DELETE, uri"$baseUrl/emails")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[Unit])

  /**
   * Deletes a single email from account scope. Operation is destructive and not reversible.
   * 
   * Expected answers:
   *   code 204 :  (No Content)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email to delete
   */
  def deleteEmail(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], Unit], Nothing] =
    basicRequest
      .method(Method.DELETE, uri"$baseUrl/emails/${emailId}")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[Unit])

  /**
   * Returns attachment bytes by attachment ID. Use attachment IDs from email payloads or attachment listing endpoints.
   * 
   * Expected answers:
   *   code 0 : ByteArray (default response)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email
   * @param attachmentId ID of attachment
   * @param apiKey Can pass apiKey in url for this request if you wish to download the file in a browser. Content type will be set to original content type of the attachment file. This is so that browsers can download the file correctly.
   */
  def downloadAttachment(apiKey: String)(emailId: UUID, attachmentId: String, apiKey: Option[String] = None
): Request[Either[ResponseError[Exception], ByteArray], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/attachments/${attachmentId}?apiKey=${ apiKey }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[ByteArray])

  /**
   * Returns attachment payload as base64 in JSON. Useful for clients that cannot reliably consume binary streaming responses.
   * 
   * Expected answers:
   *   code 200 : DownloadAttachmentDto (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email
   * @param attachmentId ID of attachment
   */
  def downloadAttachmentBase64(apiKey: String)(emailId: UUID, attachmentId: String
): Request[Either[ResponseError[Exception], DownloadAttachmentDto], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/attachments/${attachmentId}/base64")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[DownloadAttachmentDto])

  /**
   * Returns hydrated email body text as a string with content type aligned to the underlying body format.
   * 
   * Expected answers:
   *   code 200 : String (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email
   */
  def downloadBody(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], String], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/body")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[String])

  /**
   * Streams hydrated email body bytes with content type derived from the message body format.
   * 
   * Expected answers:
   *   code 0 : ByteArray (default response)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email
   */
  def downloadBodyBytes(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], ByteArray], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/body-bytes")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[ByteArray])

  /**
   * Forwards an existing email to new recipients. Uses the owning inbox context unless overridden by allowed sender options.
   * 
   * Expected answers:
   *   code 201 : SentEmailDto (Created)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email
   * @param forwardEmailOptions 
   */
  def forwardEmail(apiKey: String)(emailId: UUID, forwardEmailOptions: ForwardEmailOptions
): Request[Either[ResponseError[Exception], SentEmailDto], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/${emailId}/forward")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .body(forwardEmailOptions)
      .response(asJson[SentEmailDto])

  /**
   * Returns metadata for a specific attachment ID (name, content type, and size fields).
   * 
   * Expected answers:
   *   code 200 : AttachmentMetaData (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email
   * @param attachmentId ID of attachment
   */
  def getAttachmentMetaData(apiKey: String)(emailId: UUID, attachmentId: String
): Request[Either[ResponseError[Exception], AttachmentMetaData], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/attachments/${attachmentId}/metadata")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[AttachmentMetaData])

  /**
   * Returns parsed email content including headers and body fields. Accessing hydrated content may mark the email as read depending on read-state rules.
   * 
   * Expected answers:
   *   code 200 : Email (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   */
  def getEmail(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], Email], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[Email])

  /**
   * Returns metadata for all attachment IDs associated with the email (name, content type, size, and IDs).
   * 
   * Expected answers:
   *   code 200 : Seq[AttachmentMetaData] (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email
   */
  def getEmailAttachments(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], Seq[AttachmentMetaData]], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/attachments")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[Seq[AttachmentMetaData]])

  /**
   * Extracts one-time passcodes and similar tokens from email content using the selected extraction method and fallback options.
   * 
   * Expected answers:
   *   code 200 : ExtractCodesResult (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email to extract codes from
   * @param extractCodesOptions 
   */
  def getEmailCodes(apiKey: String)(emailId: UUID, extractCodesOptions: Option[ExtractCodesOptions] = None
): Request[Either[ResponseError[Exception], ExtractCodesResult], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/${emailId}/codes")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .body(extractCodesOptions)
      .response(asJson[ExtractCodesResult])

  /**
   * Executes a Java regex pattern over hydrated email body text and returns the full match plus capture groups. Pattern syntax follows Java `Pattern` rules.
   * 
   * Expected answers:
   *   code 200 : EmailContentMatchResult (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email to match against
   * @param contentMatchOptions 
   */
  def getEmailContentMatch(apiKey: String)(emailId: UUID, contentMatchOptions: ContentMatchOptions
): Request[Either[ResponseError[Exception], EmailContentMatchResult], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/${emailId}/contentMatch")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .body(contentMatchOptions)
      .response(asJson[EmailContentMatchResult])

  /**
   * Extracts one MIME body part by `contentType` and optional `index`, returned in a structured DTO with metadata.
   * 
   * Expected answers:
   *   code 200 : EmailContentPartResult (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email to match against
   * @param contentType Content type
   * @param strict Strict content type match
   * @param index Index of content type part if multiple
   */
  def getEmailContentPart(apiKey: String)(emailId: UUID, contentType: String, strict: Option[Boolean] = None, index: Option[Int] = None
): Request[Either[ResponseError[Exception], EmailContentPartResult], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/contentPart?contentType=${ contentType }&strict=${ strict }&index=${ index }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailContentPartResult])

  /**
   * Extracts one MIME body part by `contentType` and optional `index`, and returns raw content with matching response content type when valid.
   * 
   * Expected answers:
   *   code 200 : String (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email to match against
   * @param contentType Content type
   * @param strict Strict content type match
   * @param index Index of content type part if multiple. Starts from 0 and applies to the result list after selecting for your content type. Content type parts are sorted by order found in original MIME message.
   */
  def getEmailContentPartContent(apiKey: String)(emailId: UUID, contentType: String, strict: Option[Boolean] = None, index: Option[Int] = None
): Request[Either[ResponseError[Exception], String], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/contentPart/raw?contentType=${ contentType }&strict=${ strict }&index=${ index }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[String])

  /**
   * Returns total email count for the authenticated user, or count scoped to a specific inbox when `inboxId` is provided.
   * 
   * Expected answers:
   *   code 200 : CountDto (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param inboxId 
   */
  def getEmailCount(apiKey: String)(inboxId: Option[UUID] = None
): Request[Either[ResponseError[Exception], CountDto], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/emails/count?inboxId=${ inboxId }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[CountDto])

  /**
   * Returns hydrated HTML body directly with `text/html` content type. Supports temporary access/browser usage and optional CID replacement for inline asset rendering.
   * 
   * Expected answers:
   *   code 200 : String (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   * @param replaceCidImages 
   */
  def getEmailHTML(apiKey: String)(emailId: UUID, replaceCidImages: Option[Boolean] = None
): Request[Either[ResponseError[Exception], String], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/html?replaceCidImages=${ replaceCidImages }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[String])

  /**
   * Returns hydrated HTML body and subject in a JSON DTO. Useful for API clients that need structured response payloads instead of raw HTML responses.
   * 
   * Expected answers:
   *   code 200 : EmailHtmlDto (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   * @param replaceCidImages 
   */
  def getEmailHTMLJson(apiKey: String)(emailId: UUID, replaceCidImages: Option[Boolean] = None
): Request[Either[ResponseError[Exception], EmailHtmlDto], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/html/json?replaceCidImages=${ replaceCidImages }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailHtmlDto])

  /**
   * Applies a JSoup/CSS selector to hydrated HTML email body and returns matching text lines.
   * 
   * Expected answers:
   *   code 200 : EmailTextLinesResult (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email to perform HTML query on
   * @param htmlSelector HTML selector to search for. Uses JQuery/JSoup/CSS style selector like '.my-div' to match content. See https://jsoup.org/apidocs/org/jsoup/select/Selector.html for more information.
   */
  def getEmailHTMLQuery(apiKey: String)(emailId: UUID, htmlSelector: String
): Request[Either[ResponseError[Exception], EmailTextLinesResult], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/htmlQuery?htmlSelector=${ htmlSelector }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailTextLinesResult])

  /**
   * Parses HTML content and extracts link URLs (`href`). For non-HTML emails this endpoint returns a validation error.
   * 
   * Expected answers:
   *   code 200 : EmailLinksResult (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email to fetch text for
   * @param selector Optional HTML query selector for links
   */
  def getEmailLinks(apiKey: String)(emailId: UUID, selector: Option[String] = None
): Request[Either[ResponseError[Exception], EmailLinksResult], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/links?selector=${ selector }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailLinksResult])

  /**
   * Returns precomputed URLs for preview and raw message access for the specified email.
   * 
   * Expected answers:
   *   code 200 : EmailPreviewUrls (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   */
  def getEmailPreviewURLs(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], EmailPreviewUrls], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/urls")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailPreviewUrls])

  /**
   * Renders the email in a browser engine and returns PNG data as base64. Useful for APIs and dashboards that cannot easily stream binary responses.
   * 
   * Expected answers:
   *   code 200 : EmailScreenshotResult (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   * @param getEmailScreenshotOptions 
   */
  def getEmailScreenshotAsBase64(apiKey: String)(emailId: UUID, getEmailScreenshotOptions: GetEmailScreenshotOptions
): Request[Either[ResponseError[Exception], EmailScreenshotResult], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/${emailId}/screenshot/base64")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .body(getEmailScreenshotOptions)
      .response(asJson[EmailScreenshotResult])

  /**
   * Renders the email in a browser engine and returns PNG bytes. Intended for visual QA and rendering regression checks.
   * 
   * Expected answers:
   *   code 200 :  (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   * @param getEmailScreenshotOptions 
   */
  def getEmailScreenshotAsBinary(apiKey: String)(emailId: UUID, getEmailScreenshotOptions: GetEmailScreenshotOptions
): Request[Either[ResponseError[Exception], Unit], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/${emailId}/screenshot/binary")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .body(getEmailScreenshotOptions)
      .response(asJson[Unit])

  /**
   * Attempts to parse a sender signature block from an email body. Uses raw MIME content parts when possible and falls back to hydrated body parsing. This is heuristic and may not be accurate for all email clients or formats.
   * 
   * Expected answers:
   *   code 200 : EmailSignatureParseResult (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email to extract signature from
   */
  def getEmailSignature(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], EmailSignatureParseResult], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/signature")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailSignatureParseResult])

  /**
   * Returns lightweight metadata and headers for an email. Use `getEmail` for hydrated body content or `getRawEmail` for original SMTP content.
   * 
   * Expected answers:
   *   code 200 : EmailPreview (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   * @param decode Deprecated and ignored. Retained for backwards compatibility.
   */
  def getEmailSummary(apiKey: String)(emailId: UUID, decode: Option[Boolean] = None
): Request[Either[ResponseError[Exception], EmailPreview], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/summary?decode=${ decode }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailPreview])

  /**
   * Converts email body content to line-based plain text with optional HTML entity decoding and custom line separator.
   * 
   * Expected answers:
   *   code 200 : EmailTextLinesResult (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email to fetch text for
   * @param decodeHtmlEntities Decode HTML entities
   * @param lineSeparator Line separator character
   */
  def getEmailTextLines(apiKey: String)(emailId: UUID, decodeHtmlEntities: Option[Boolean] = None, lineSeparator: Option[String] = None
): Request[Either[ResponseError[Exception], EmailTextLinesResult], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/textLines?decodeHtmlEntities=${ decodeHtmlEntities }&lineSeparator=${ lineSeparator }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailTextLinesResult])

  /**
   * Returns thread metadata built from RFC 5322 `Message-ID`, `In-Reply-To`, and `References` headers. Use `getEmailThreadItems` to fetch the thread messages.
   * 
   * Expected answers:
   *   code 200 : EmailThreadDto (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param threadId 
   */
  def getEmailThread(apiKey: String)(threadId: UUID
): Request[Either[ResponseError[Exception], EmailThreadDto], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/threads/${threadId}")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailThreadDto])

  /**
   * Returns all messages in a thread ordered by `createdAt` using the requested sort direction.
   * 
   * Expected answers:
   *   code 200 : EmailThreadItemsDto (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param threadId 
   * @param sort Optional createdAt sort direction ASC or DESC
   */
  def getEmailThreadItems(apiKey: String)(threadId: UUID, sort: Option[String] = None
): Request[Either[ResponseError[Exception], EmailThreadItemsDto], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/threads/${threadId}/items?sort=${ sort }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailThreadItemsDto])

  /**
   * Lists conversation threads inferred from `Message-ID`, `In-Reply-To`, and `References`. Supports filtering by inbox, search text, and time range.
   * 
   * Expected answers:
   *   code 200 : PageEmailThreadProjection (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param htmlSelector Optional inbox filter. Query parameter name is `htmlSelector` for legacy compatibility.
   * @param page Optional page index in email thread pagination
   * @param size Optional page size in email thread pagination. Maximum size is 100. Use page index and sort to page through larger results
   * @param sort Optional createdAt sort direction ASC or DESC
   * @param searchFilter Optional search filter search filter for email threads.
   * @param since Optional filter email threads created since time
   * @param before Optional filter emails threads created before given date time
   */
  def getEmailThreads(apiKey: String)(htmlSelector: Option[UUID] = None, page: Option[Int] = None, size: Option[Int] = None, sort: Option[String] = None, searchFilter: Option[String] = None, since: Option[OffsetDateTime] = None, before: Option[OffsetDateTime] = None
): Request[Either[ResponseError[Exception], PageEmailThreadProjection], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/threads?htmlSelector=${ htmlSelector }&page=${ page }&size=${ size }&sort=${ sort }&searchFilter=${ searchFilter }&since=${ since }&before=${ before }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[PageEmailThreadProjection])

  /**
   * Offset-style pagination endpoint for listing emails across inboxes. Supports inbox filters, unread-only, search, date boundaries, favourites, connector sync, plus-address filtering, and explicit include IDs.
   * 
   * Expected answers:
   *   code 200 : PageEmailProjection (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param inboxId Optional inbox ids to filter by. Can be repeated. By default will use all inboxes belonging to your account.
   * @param page Optional page index in email list pagination
   * @param size Optional page size in email list pagination. Maximum size is 100. Use page index and sort to page through larger results
   * @param sort Optional createdAt sort direction ASC or DESC
   * @param unreadOnly Optional filter for unread emails only. All emails are considered unread until they are viewed in the dashboard or requested directly
   * @param searchFilter Optional search filter. Full email addresses match sender and receiving inbox email or receiving plus-address full address exactly. Address-like fragments containing @ (for example +alias@) also match sender, receiving inbox email, and receiving plus-address full address by contains. General text search matches sender, subject, and ID.
   * @param since Optional filter emails received after given date time
   * @param before Optional filter emails received before given date time
   * @param favourited Optional filter emails that are favourited
   * @param syncConnectors Sync connectors
   * @param plusAddressId Optional plus address ID filter
   * @param include Optional list of IDs to include in result
   */
  def getEmailsOffsetPaginated(apiKey: String)(inboxId: Seq[UUID], page: Option[Int] = None, size: Option[Int] = None, sort: Option[String] = None, unreadOnly: Option[Boolean] = None, searchFilter: Option[String] = None, since: Option[OffsetDateTime] = None, before: Option[OffsetDateTime] = None, favourited: Option[Boolean] = None, syncConnectors: Option[Boolean] = None, plusAddressId: Option[UUID] = None, include: Seq[UUID]
): Request[Either[ResponseError[Exception], PageEmailProjection], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/offset-paginated?inboxId=${ inboxId }&page=${ page }&size=${ size }&sort=${ sort }&unreadOnly=${ unreadOnly }&searchFilter=${ searchFilter }&since=${ since }&before=${ before }&favourited=${ favourited }&syncConnectors=${ syncConnectors }&plusAddressId=${ plusAddressId }&include=${ include }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[PageEmailProjection])

  /**
   * Primary paginated email listing endpoint. Returns emails across inboxes with support for inbox filters, unread-only, search, date boundaries, favourites, connector sync, and plus-address filtering.
   * 
   * Expected answers:
   *   code 200 : PageEmailProjection (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param inboxId Optional inbox ids to filter by. Can be repeated. By default will use all inboxes belonging to your account.
   * @param page Optional page index in email list pagination
   * @param size Optional page size in email list pagination. Maximum size is 100. Use page index and sort to page through larger results
   * @param sort Optional createdAt sort direction ASC or DESC
   * @param unreadOnly Optional filter for unread emails only. All emails are considered unread until they are viewed in the dashboard or requested directly
   * @param searchFilter Optional search filter. Full email addresses match sender and receiving inbox email or receiving plus-address full address exactly. Address-like fragments containing @ (for example +alias@) also match sender, receiving inbox email, and receiving plus-address full address by contains. General text search matches sender, subject, and ID.
   * @param since Optional filter emails received after given date time. If unset will use time 24hours prior to now.
   * @param before Optional filter emails received before given date time
   * @param syncConnectors Sync connectors
   * @param plusAddressId Optional plus address ID filter
   * @param favourited Optional filter emails that are favourited
   */
  def getEmailsPaginated(apiKey: String)(inboxId: Seq[UUID], page: Option[Int] = None, size: Option[Int] = None, sort: Option[String] = None, unreadOnly: Option[Boolean] = None, searchFilter: Option[String] = None, since: Option[OffsetDateTime] = None, before: Option[OffsetDateTime] = None, syncConnectors: Option[Boolean] = None, plusAddressId: Option[UUID] = None, favourited: Option[Boolean] = None
): Request[Either[ResponseError[Exception], PageEmailProjection], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails?inboxId=${ inboxId }&page=${ page }&size=${ size }&sort=${ sort }&unreadOnly=${ unreadOnly }&searchFilter=${ searchFilter }&since=${ since }&before=${ before }&syncConnectors=${ syncConnectors }&plusAddressId=${ plusAddressId }&favourited=${ favourited }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[PageEmailProjection])

  /**
   * Builds a Gravatar image URL from the provided email address and optional size. This endpoint does not fetch image bytes; it only returns the computed URL.
   * 
   * Expected answers:
   *   code 200 : GravatarUrl (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailAddress 
   * @param size 
   */
  def getGravatarUrlForEmailAddress(apiKey: String)(emailAddress: String, size: Option[String] = None
): Request[Either[ResponseError[Exception], GravatarUrl], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/gravatarFor?emailAddress=${ emailAddress }&size=${ size }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[GravatarUrl])

  /**
   * Returns the most recently received email across all inboxes or an optional subset of inbox IDs.
   * 
   * Expected answers:
   *   code 200 : Email (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param inboxIds Optional set of inboxes to filter by. Only get the latest email from these inbox IDs. If not provided will search across all inboxes
   */
  def getLatestEmail(apiKey: String)(inboxIds: Seq[UUID]
): Request[Either[ResponseError[Exception], Email], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/latest?inboxIds=${ inboxIds }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[Email])

  /**
   * Returns the newest email for the specified inbox ID. For polling/wait semantics use wait endpoints.
   * 
   * Expected answers:
   *   code 200 : Email (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param inboxId ID of the inbox you want to get the latest email from
   */
  def getLatestEmailInInbox1(apiKey: String)(inboxId: UUID
): Request[Either[ResponseError[Exception], Email], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/latestIn?inboxId=${ inboxId }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[Email])

  /**
   * Returns paginated emails visible through organization/team access. Supports inbox filtering, unread-only filtering, search, favourites, plus-address filtering, and optional connector sync.
   * 
   * Expected answers:
   *   code 200 : PageEmailProjection (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param inboxId Optional inbox ids to filter by. Can be repeated. By default will use all inboxes belonging to your account.
   * @param page Optional page index in email list pagination
   * @param size Optional page size in email list pagination. Maximum size is 100. Use page index and sort to page through larger results
   * @param sort Optional createdAt sort direction ASC or DESC
   * @param unreadOnly Optional filter for unread emails only. All emails are considered unread until they are viewed in the dashboard or requested directly
   * @param searchFilter Optional search filter search filter for emails.
   * @param since Optional filter emails received after given date time. If unset will use time 24hours prior to now.
   * @param before Optional filter emails received before given date time
   * @param syncConnectors Sync connectors
   * @param favourited Search only favorited emails
   * @param plusAddressId Optional plus address ID filter
   */
  def getOrganizationEmailsPaginated(apiKey: String)(inboxId: Seq[UUID], page: Option[Int] = None, size: Option[Int] = None, sort: Option[String] = None, unreadOnly: Option[Boolean] = None, searchFilter: Option[String] = None, since: Option[OffsetDateTime] = None, before: Option[OffsetDateTime] = None, syncConnectors: Option[Boolean] = None, favourited: Option[Boolean] = None, plusAddressId: Option[UUID] = None
): Request[Either[ResponseError[Exception], PageEmailProjection], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/organization?inboxId=${ inboxId }&page=${ page }&size=${ size }&sort=${ sort }&unreadOnly=${ unreadOnly }&searchFilter=${ searchFilter }&since=${ since }&before=${ before }&syncConnectors=${ syncConnectors }&favourited=${ favourited }&plusAddressId=${ plusAddressId }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[PageEmailProjection])

  /**
   * Returns the original unparsed SMTP/MIME message as `text/plain`. Use JSON variant if your client expects JSON transport.
   * 
   * Expected answers:
   *   code 200 :  (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email
   */
  def getRawEmailContents(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], Unit], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/raw")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[Unit])

  /**
   * Returns the original unparsed SMTP/MIME message wrapped in a JSON DTO for API clients that avoid plain-text stream responses.
   * 
   * Expected answers:
   *   code 200 : RawEmailJson (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email
   */
  def getRawEmailJson(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], RawEmailJson], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/${emailId}/raw/json")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[RawEmailJson])

  /**
   * Returns unread email count. An email is considered read after dashboard/API retrieval depending on your read workflow.
   * 
   * Expected answers:
   *   code 200 : UnreadCount (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param inboxId Inbox ID filter for unread count scope
   */
  def getUnreadEmailCount(apiKey: String)(inboxId: Option[UUID] = None
): Request[Either[ResponseError[Exception], UnreadCount], Nothing] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/emails/unreadCount?inboxId=${ inboxId }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[UnreadCount])

  /**
   * Sets read state for all emails, optionally scoped to one inbox. Use `read=false` to reset unread state in bulk.
   * 
   * Expected answers:
   *   code 204 :  (No Content)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param read What value to assign to email read property. Default true.
   * @param inboxId Optional inbox ID filter
   */
  def markAllAsRead(apiKey: String)(read: Option[Boolean] = None, inboxId: Option[UUID] = None
): Request[Either[ResponseError[Exception], Unit], Nothing] =
    basicRequest
      .method(Method.PATCH, uri"$baseUrl/emails/read?read=${ read }&inboxId=${ inboxId }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[Unit])

  /**
   * Sets read state for one email. Useful when implementing custom mailbox workflows that treat viewed messages as unread.
   * 
   * Expected answers:
   *   code 200 : EmailPreview (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId 
   * @param read What value to assign to email read property. Default true.
   */
  def markAsRead(apiKey: String)(emailId: UUID, read: Option[Boolean] = None
): Request[Either[ResponseError[Exception], EmailPreview], Nothing] =
    basicRequest
      .method(Method.PATCH, uri"$baseUrl/emails/${emailId}/read?read=${ read }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[EmailPreview])

  /**
   * Sends a reply using the original conversation context (subject/thread headers). Reply target resolution honors sender/reply-to semantics.
   * 
   * Expected answers:
   *   code 200 : SentEmailDto (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of the email that should be replied to
   * @param replyToEmailOptions 
   */
  def replyToEmail(apiKey: String)(emailId: UUID, replyToEmailOptions: ReplyToEmailOptions
): Request[Either[ResponseError[Exception], SentEmailDto], Nothing] =
    basicRequest
      .method(Method.PUT, uri"$baseUrl/emails/${emailId}")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .body(replyToEmailOptions)
      .response(asJson[SentEmailDto])

  /**
   * Searches emails by sender/recipient/address/subject/id fields and returns paginated matches. Does not perform full-text body search.
   * 
   * Expected answers:
   *   code 200 : PageEmailProjection (OK)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param searchEmailsOptions 
   * @param syncConnectors Sync connectors
   * @param favourited Search only favourited emails
   * @param plusAddressId Optional plus address ID filter
   */
  def searchEmails(apiKey: String)(searchEmailsOptions: SearchEmailsOptions, syncConnectors: Option[Boolean] = None, favourited: Option[Boolean] = None, plusAddressId: Option[UUID] = None
): Request[Either[ResponseError[Exception], PageEmailProjection], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/search?syncConnectors=${ syncConnectors }&favourited=${ favourited }&plusAddressId=${ plusAddressId }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .body(searchEmailsOptions)
      .response(asJson[PageEmailProjection])

  /**
   * Sends an email from an existing inbox, or creates a temporary inbox when `inboxId` is not provided. Supports `useDomainPool` and `virtualSend` inbox creation behavior for convenience sends.
   * 
   * Expected answers:
   *   code 201 :  (Created)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param sendEmailOptions 
   * @param inboxId ID of the inbox you want to send the email from
   * @param useDomainPool Use domain pool. Optionally create inbox to send from using the mailslurp domain pool.
   * @param virtualSend Optionally create inbox to send from that is a virtual inbox and won't send to external addresses
   */
  def sendEmailSourceOptional(apiKey: String)(sendEmailOptions: SendEmailOptions, inboxId: Option[UUID] = None, useDomainPool: Option[Boolean] = None, virtualSend: Option[Boolean] = None
): Request[Either[ResponseError[Exception], Unit], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails?inboxId=${ inboxId }&useDomainPool=${ useDomainPool }&virtualSend=${ virtualSend }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .body(sendEmailOptions)
      .response(asJson[Unit])

  /**
   * Sets favourite state for an email for dashboard/search workflows.
   * 
   * Expected answers:
   *   code 204 :  (No Content)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email to set favourite state
   * @param favourited 
   */
  def setEmailFavourited(apiKey: String)(emailId: UUID, favourited: Boolean
): Request[Either[ResponseError[Exception], Unit], Nothing] =
    basicRequest
      .method(Method.PUT, uri"$baseUrl/emails/${emailId}/favourite?favourited=${ favourited }")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[Unit])

  /**
   * Runs HTML validation on the email body when HTML is present. Non-HTML emails are treated as valid for this check.
   * 
   * Expected answers:
   *   code 201 : ValidationDto (Created)
   * 
   * Available security schemes:
   *   API_KEY (apiKey)
   * 
   * @param emailId ID of email
   */
  def validateEmail(apiKey: String)(emailId: UUID
): Request[Either[ResponseError[Exception], ValidationDto], Nothing] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/emails/${emailId}/validate")
      .contentType("application/json")
      .header("x-api-key", apiKey)
      .response(asJson[ValidationDto])

}
